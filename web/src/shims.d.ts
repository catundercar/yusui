// Side-effect-only CSS packages that ship no bundled type declarations; declared
// here so `vue-tsc --noEmit` (the CI type check) resolves their imports.
declare module "@fontsource-variable/geist"
declare module "@fontsource-variable/geist-mono"
